Modifications to Raise new work for M&C record types 
The new order type is an option for user to select
Show the fields related to selecting the Vendor -Contractor group and Paving code (Service item) (Input needed from Nick).
When selected modify the flow to ask the questions for restoration request flow. Set attribute flag to true - same day restoration
Show the question - do you want to add another same day restoration request. If yes, iterate through the restoration request flow to capture new data.
All for multiple same day restoration request records to be created as WOLI to the parent same day restoration workorder.Create the Same day restoration workorder and WOLI in complete status (Let's discuss on this)
